name: Build CWR
description: Configure, build and test a CWR/Poseidon CMake preset.

inputs:
  preset:
    description: CMake configure preset to build.
    required: true
  build-dir:
    description: Build directory the preset writes to.
    required: true
  ctest-exclude:
    description: Regex of tests to exclude (ctest -E). Empty runs all.
    required: false
    default: ''
  run-tests:
    description: Run ctest after the build.
    required: false
    default: 'true'
  use-ccache:
    description: Enable the ccache compiler launcher and cache.
    required: false
    default: 'true'
  ccache-max-size:
    description: Size limit for the ccache cache.
    required: false
    default: '1G'
  extra-cmake-args:
    description: Extra arguments for the configure command.
    required: false
    default: ''

runs:
  using: composite
  steps:
    - name: Install Linux system packages
      if: runner.os == 'Linux'
      uses: ./.github/actions/setup-linux-deps

    - name: Install macOS homebrew packages
      if: runner.os == 'macOS'
      shell: bash
      run: |
        brew install \
          clang-format

    - name: Setup LLVM and Clang
      if: runner.os == 'Windows'
      uses: KyleMayes/install-llvm-action@v2
      with:
        version: '19'
        arch: 'x64'
        directory: 'C:\Program Files\LLVM'

    - name: Setup compiler cache
      if: inputs.use-ccache == 'true'
      uses: hendrikmuhs/ccache-action@v1.2.23
      with:
        variant: ccache
        key: ${{ runner.os }}-${{ inputs.preset }}-pch-${{ hashFiles('engine/Poseidon/Foundation/PoseidonPCH.hpp', 'engine/Poseidon/Foundation/platform.hpp') }}
        max-size: ${{ inputs.ccache-max-size }}

    - name: Setup CMake and Ninja
      uses: lukka/get-cmake@latest
      with:
        cmakeVersion: '4.4.0'

    - name: Install Conan
      uses: conan-io/setup-conan@v1

    - name: Configure
      shell: bash
      run: >
        cmake
        --preset ${{ inputs.preset }}
        -DCMAKE_C_COMPILER_LAUNCHER=${{ inputs.use-ccache == 'true' && 'ccache' || '' }}
        -DCMAKE_CXX_COMPILER_LAUNCHER=${{ inputs.use-ccache == 'true' && 'ccache' || '' }}
        ${{ inputs.extra-cmake-args }}

    - name: Build
      shell: bash
      run: cmake --build ${{ inputs.build-dir }} --parallel 2

    - name: Test
      if: inputs.run-tests == 'true'
      shell: pwsh
      env:
        CTEST_EXCLUDE: ${{ inputs.ctest-exclude }}
        ALSOFT_DRIVERS: "null"
      run: |
        if ("$env:CTEST_EXCLUDE") {
          ctest --test-dir '${{ inputs.build-dir }}' --output-on-failure -E "$env:CTEST_EXCLUDE"
        } else {
          ctest --test-dir '${{ inputs.build-dir }}' --output-on-failure
        }
